# App configuration for the vacations app.
from django.apps import AppConfig

# Configuration class for the vacations app.
class VacationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.vacations'
